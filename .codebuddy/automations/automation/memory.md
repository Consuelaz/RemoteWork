# 自动化任务执行记录

## 最新执行

- **日期**: 2026-03-24
- **脚本**: `scrape.sh`（工作目录中无 `scraper.py`，实际抓取脚本为 `scrape.sh`）
- **抓取结果**:
  - Remote OK: 50 个新岗位（成功）
  - 远程中文网: 15 个新岗位（成功）
  - V2EX: 20 个新岗位（成功，代理可用）
  - 电鸭: 0 个岗位（选择器未匹配）
- **数据合并**: CN 145→180（新增35条），Global 200（已达上限，无变化）
- **Git 提交**: `202d582`，已推送至 `master`（commit: "自动更新: 2026-03-24"）

## 执行历史

| 日期 | CN岗位数 | Global岗位数 | 状态 |
|------|----------|--------------|------|
| 2026-03-24 | 180 | 200 | ✅ 成功（V2EX可用） |
| 2026-03-23 | 145 | 200 | ✅ 成功（V2EX可用） |
| 2026-03-22 | 110 | 200 | ✅ 成功（V2EX可用） |
| 2026-03-21 | 75 | 200 | ✅ 成功 |

## Bug 修复记录

- **2026-03-21 修复**：`scrape.sh` 中 `jobs_to_js()` 函数将 Python 布尔值 `True/False` 直接写入 JS，导致 JS 语法错误、网站无数据显示。已修复为输出 `true/false`（JS 格式）。同时修复了存量数据文件（`jobs-cn.js`、`jobs-global.js`）中的 255 处错误。

## 注意事项

- V2EX 需要代理（`http://127.0.0.1:7897`），代理未启动时抓取失败属正常现象；近三日代理可用，成功抓取 20 条
- 电鸭页面选择器（`div.post-item` 等）可能需要更新以匹配当前页面结构
- `requirements.txt` 不存在，项目依赖 `beautifulsoup4`、`requests`，均已安装
- Global 岗位数设上限为 200，Remote OK 每次刷新数据（非累积）
