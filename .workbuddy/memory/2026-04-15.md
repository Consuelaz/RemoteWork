# 2026-04-15 日记

## AI News 样式全面对标 2026-04-13.html（定型版）

### 变更内容
- **gen-index.py 完全重写**：采用参考页面的完整 HTML 结构
  - Navbar：顶栏含导航链接 + 中英切换 + 全文朗读（集成在一行）
  - Hero：蓝色渐变大头部（#1E3A5F → #2563EB）
  - Section 标题：🐦 建造者动态 / 🎙️ 播客 / 📝 官方博客
  - 建造者卡片：头像（渐变紫色）+ handle + bio + 多推文列表
  - 播客卡片：标题 + 来源标签 + 黄色背景字幕摘要区
  - 博客卡片：来源 + 标题 + 阅读链接
  - 历史存档：最近10天
- **中英文切换**：navbar 右侧中文/EN 按钮，推文/播客/博客内容均支持切换
- **git 自动推送**：commit msg 含建造者/推文/播客/博客数量
- MEMORY.md 已更新，将此页面结构写为硬性规范

### Bug 修复：语言切换初始化
- **问题**：语言切换按钮点击无效
- **根因**：`.click()` 模拟点击顺序在 `loadVoices()` 调用中间，且依赖 `btn.click()` 触发异步逻辑
- **修复**：语言初始化改为 IIFE `(function initLang(){...})()` 立即执行，与语音加载完全解耦

---

## 移动端导航栏修复

### 问题
手机浏览器看不到导航链接（岗位列表、100+公司列表、AI资讯、加入社群）。
根因：`style.css` 的 `@media (max-width:768px)` 中有 `.nav-links { display: none; }` 直接隐藏了导航，且没有汉堡菜单替代方案。

### 修复内容
1. **style.css**：删除 `display: none`，改为汉堡菜单折叠交互
   - 新增 `.nav-toggle` 汉堡按钮样式（三横线 → X 动画）
   - 手机端 `.nav-links` 折叠为绝对定位下拉菜单（max-height 动画展开）
   - 手机端 `.nav-cta`（"发布岗位" 按钮）隐藏
2. **index.html**：加入 `#navToggle` 汉堡按钮 + `toggleNav()` JS
3. **companies.html**：同上，补充了缺失的 "AI资讯" 导航链接
4. **community.html**：同上，补充了缺失的 "AI资讯" 导航链接
5. **ai-news/index.html**：加 `@media` 块，手机端缩减 padding，优化 controls 布局
