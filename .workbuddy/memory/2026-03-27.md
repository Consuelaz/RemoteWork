# 2026-03-27 工作日志

## 每日数据抓取（自动化任务 08:00）

- **脚本**: `scrape.sh`
- **抓取结果**:
  - Remote OK: 94 条 ✅
  - Remotive: 21 条 ✅
  - 远程岛: 150 条 ✅
  - 远程中文网: 15 条 ✅
  - V2EX: 20 条 ✅（代理可用）
  - 电鸭: 跳过（文件不存在）
- **数据变化**: CN 689→724（+35），Global 1815→2000（+185）

## V2EX 抓取三项深度优化（15:21）

### 变更内容
1. **只抓当天帖子**：从 `span[title]` 的 `title` 属性提取发帖时间（格式 `2026-03-27 14:33:01 +08:00`），取前10位与 DATE 比较，非今天跳过
2. **公司名提取增强**：`parse_v2ex_detail()` 新增从正文提取，支持：`【公司名】`、`公司：xxx`、`我司是xxx`、`关于我们：xxx`；V2EX 处理段优先用详情页结果，标题模式兜底，最终无法提取填 "海外公司"（原来填"（V2EX用户招聘）"）
3. **工作职责兜底**：结构化字段（岗位职责/任职要求）为空时，取正文所有行过滤联系方式后写入（最多30行）；使用 `.topic_content` 替代 `#Main` 避免抓到回复内容
- Git 提交: `03a3481`
- 今日V2EX: 3条（跳过17条非今日）


### 变更内容
1. **地区** → 统一填 "Remote"
2. **申请链接** → 固定填 "内推岗"
3. **内推** → 固定填 "星球同学 私Junes内推"
4. **工作职责** → 抓取岗位职责+任职要求，并过滤掉含邮箱/微信/电话的联系方式行
5. **内推方式** → 从 V2EX 详情页提取联系方式（邮箱/微信/电话），抓不到才填默认值
6. **来源** → 原帖 URL
7. **公司** → 智能提取，抓不到填 "海外公司"

### 新增逻辑（scrape.sh）
- `parse_v2ex_detail()` 新增 `contact` 字段提取（邮箱/微信/wx/wechat/电话/Telegram）
- V2EX job 字典新增 `contact` 字段
- `build_job_desc()` 函数：合并职责+要求，逐行过滤联系方式
- `extract_company_from_text()` 函数：从正文和标题多重提取公司名
- Git 提交: `8b3ee3f`

## 数据去重（16:29）

- **历史数据去重**：jobs-cn.js 762→78 条（删除 684 条），jobs-global.js 2000→808 条（删除 1192 条）
- **去重唯一键**：`sourceUrl`（不用 id，因为同一岗位多次抓取 id 可能不同）
- **scrape.sh**：`merge_jobs()` 改为先合并新旧（新优先），再用 `dedup_by_source_url()` 全局去重
- Git 提交: 待提交

## 数据显示问题修复（16:19）

### 问题
1. 列表页远程中文网岗位公司名显示"远程中文网"
2. 详情页岗位描述显示"来自远程中文网的远程工作机会"占位语
3. 详情页 responsibilities 中出现 `https://v2ex.com/t/xxx` 原帖链接文字

### 修改内容
- **app.js**：
  - `formatCompany()` 新增 `INVALID_COMPANY_NAMES` 数组，过滤"远程中文网"等数据源名称
  - `validDescription` 改为 `INVALID_DESCRIPTIONS` 数组过滤，不显示无效描述区块
  - 新增 `filterSourceLines()` 函数，过滤 responsibilities/requirements 中含 v2ex.com / remote-china.com 的行
- **scrape.sh**：
  - `parse_remotechina_detail()` 全面扩展，从 `.markdown-content` 提取真实岗位描述/职责/要求
  - 远程中文网 job 构建：company 兜底改为 `""`，description 改为从详情页提取
- **jobs-cn.js**：批量清理历史数据（公司名 27 处、description 占位符 335 处）



### 变更内容
1. **jobs-cn.js**：批量删除 15 条垃圾帖子（"一直想做的远程工作集合网站，上线了" ×3、"远程工作网站开放" ×12）
2. **scrape.sh**：在 V2EX 列表处理段（时间过滤之前）新增 `TITLE_BLACKLIST` 列表过滤，匹配到关键词则跳过，不写入数据文件和 money.xlsx
   - 已加入黑名单：`一直想做的远程工作集合网站`、`远程工作网站开放`
   - 代码位置：`scrape.sh` V2EX 列表处理段，行号约 964-971

