# 2026-06-26 工作记录

## 手动运行 scrape.sh 抓取职位数据

- **触发**：用户手动要求运行 `bash scrape.sh` 并推送到 GitHub
- **代理**：`http://127.0.0.1:7892` 可用，V2EX 抓取正常
- **数据源结果**：
  - Remote OK: 100 条
  - Remotive: 32 条
  - 远程岛: 150 条
  - who-is-hiring: 429 条（含联系方式 429 条）
  - 远程中文网: 15 条
  - V2EX: 0 条新帖（跳过 20 个非今日帖子）
  - 电鸭: 跳过（JS 渲染无法抓取）
- **数据变化**：CN 1019→1034 (+15)，Global 2000 (不变)
- **Git**：commit `218f7ed`，push 成功
- **money.xlsx**：更新成功（0 条 V2EX + 429 条 who-is-hiring）

## 修复 scrape.sh SSH 推送挂起

- 现象：`git push` 后生成 `ssh: github.com-22-git [mux]` 并挂起
- 处理：手动杀掉 mux 进程后推送成功
- 修复：在 `scrape.sh` 中增加 `export GIT_SSH_COMMAND="ssh -o ControlMaster=no"`，避免后续自动化任务挂起
- 修复：将 `git add` 中的 `.codebuddy/automations/automation/memory.md` 改为 `.workbuddy/automations/automation/memory.md`
