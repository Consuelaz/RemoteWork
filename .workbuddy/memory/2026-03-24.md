# 2026-03-24 工作日志

## 已完成工作

- 启动本地预览服务器（`python3 -m http.server 8080`）
- 修复 `scrape.sh` 中 Remote OK `sourceUrl` 重复拼接 bug（`https://remoteok.comhttps://...`）
  - 修复 `scrape.sh` 脚本逻辑
  - 批量修复 `jobs-global.js` 已有的 100 处错误 URL
- 创建 `MEMORY.md` 记录项目关键 bug 和架构信息

### V2EX 数据抓取与 money.xlsx 同步（2026-03-24 新增）
- 抓取 V2EX 远程工作板块数据（17条）
- 更新 `jobs-cn.js`：V2EX 数据标记为"社群内推"，放在列表顶部
- 更新 `money.xlsx`：V2EX 数据插入表格顶部（公司、行业、职位类别、职位名称、地区、申请链接）
- 更新 `scrape.sh`：添加 money.xlsx 自动更新逻辑

### 岗位显示顺序与详情丰富化（2026-03-24 下午）
- 修复 `app.js` 中 `getCurrentJobs()` 函数，按日期倒序排列，最新岗位排在前面
- 移除列表中"xxx 发布"的显示格式
- 修改内推标签为"👥 内推岗"
- 增强 V2EX 详情页抓取：预下载前7个岗位详情页，提取岗位职责、任职要求、福利待遇、公司背景、薪资、工作方式
- 修复详情页 URL 映射问题：通过 URL 映射而非索引匹配
- 更新弹窗详情展示新增字段（公司介绍、岗位职责等）

### 列表展示与详情页占位符修复（2026-03-24 13:44）
- 列表中过滤掉 "V2EX"、"远程"、"社群内推" 等冗余标签，只保留"内推岗"标识
- 详情页中公司介绍/岗位描述字段如果内容少于10个字符，不显示该区块
- 修复 scrape.sh 中 description 生成逻辑，避免无意义占位符（如"公司背景"）
- 清理 jobs-cn.js 老数据中的无效占位符

### 用户反馈问题修复（2026-03-24 14:17）
- 修复中国远程岗列表显示问题：
  - 职位名称截断：超过28字符显示"..."
  - 公司名显示优化：截断过长名称，V2EX统一显示"V2EX用户招聘"
  - 详情页标题截断：超过50字符显示"..."
- 优化 V2EX 公司名提取逻辑：
  - 多种正则模式匹配公司名
  - 黑名单过滤无效公司名（如"远程"、"全职远程"等）
- 增加 Remote OK 详情页抓取：
  - 预下载前10个海外岗位详情页
  - 提取职位描述、任职要求、福利待遇
- 创建 skill：`~/.workbuddy/skills/junes-remote-jobs/`

### 移除 V2EX 相关显示文字（2026-03-24 14:34）
- 移除列表页和详情页中的"（V2EX用户招聘）"字样
- 修复 `formatCompany()` 函数作用域问题（移到全局）
- 修复详情页"来自V2EX远程工作社区的招聘帖子"无效描述，用职位标题替代
- 详情页点击功能修复（函数作用域问题）

### 智能提取职位名称（2026-03-24 14:53）
- 新增 `extractJobTitle()` 函数，从完整标题中提取核心职位名
- 支持 50+ 职位类型匹配（工程师、设计师、运营、产品、经理等）
- 按优先级排序：高级职位 > 管理职位 > 技术职位 > 其他
- 未匹配时截取前10个字符
- 已写入长期记忆 MEMORY.md

### 数据限制扩展与新数据源（2026-03-24 15:21）
- 修改 `scrape.sh` 数据抓取限制：
  - V2EX: 30条 → 200条
  - 远程中文网: 30条 → 2000条
  - Remote OK: 50条 → 2000条
  - 国内总计: 200条 → 2000条
  - 海外总计: 200条 → 2000条
  - 合并上限: 5000条
- 新增 Remotive API 数据源：`https://remotive.com/api/remote-jobs`
- 更新 Skill：`~/.workbuddy/skills/junes-remote-jobs/`
  - 添加完整的数据源列表
  - 添加如何扩展新数据源的步骤指南
  - 记录待添加数据源（We Work Remotely、Jobspresso 等）
